new VenoBox({
    selector: '.my-image-links',
    numeration: true,
    infinigall: true,
    share: true,
    spinner: 'rotating-plane'
});

$(".designer-slider").slick({
    slidesToShow : 3,
    arrows:false,
    autoplay: true,
});

new VenoBox({
    selector: '.my-video-links',
});